/* @flow weak */

"use strict";

var React = require('react');

var Add = React.createClass({

  render: function() {
    return (
      <div>
        <a href="/">Events List</a>
        Add
      </div>
    );
  }

});

module.exports = Add;
