/* @flow weak */

"use strict";

var React = require('react')

var Event = React.createClass({

  render: function() {
    return (
      <div>
        Event View
      </div>
    );
  }

});

module.exports = Event;
